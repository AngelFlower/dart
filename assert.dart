void main(List<String> args) {
  double numerador = 10, denominador = 0;

  assert(denominador != denominador, 'No podre hacer la división');
  print('División = ${numerador / denominador}');
  /*
  if (0 != denominador) {
    print('División = ${numerador / denominador}');
  } else {
    print('La división entre 0 no está definida');
  }
  */
}
